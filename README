Calculadora de números romanos con Flex y Bison.
Acepta las operaciones + - * / y ( )
Las entradas se colocan en el archivo "input"

Para compilar y ejecutar:
bison -d calc.y;flex calc.l;gcc calc.tab.c lex.yy.c -lfl -o calc;./calc